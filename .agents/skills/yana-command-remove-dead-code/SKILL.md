---
name: yana-command-remove-dead-code
description: "Yana AI /remove-dead-code command adapter. Safely scan, remove, and validate dead code with multi-agent analysis and automatic backup branches"
---

# Yana AI Command: /remove-dead-code

Invoke this workflow explicitly as `$yana-command-remove-dead-code`.
Treat text supplied with the invocation as `$ARGUMENTS` wherever the source workflow references it.
Follow the source workflow without weakening its approval, scope, safety, or verification requirements.

Invoke the remove-dead-code skill for: $ARGUMENTS
