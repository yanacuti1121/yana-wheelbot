# Yana AI — project-scoped Codex configuration
# Codex loads this file only after the repository is trusted.

[features]
hooks = true

[agents]
enabled = true
max_concurrent_threads_per_session = 3
interrupt_message = true
